import { PreviewFrame } from "@/components/preview-frame";

export default function Page() {
  return (
    <PreviewFrame
      showThemeToggle={false}
      src="/progressive-blur-gallery/component"
    />
  );
}

import { PreviewFrame } from "@/components/preview-frame";

export default function Page() {
  return (
    <PreviewFrame
      showThemeToggle={false}
      src="/table-filter-buttons/component"
    />
  );
}
